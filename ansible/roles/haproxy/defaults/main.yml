---
haproxy_stats_port: "{{ proxy_admin_port | default(7000) }}"
haproxy_stats_user: "admin"
haproxy_stats_password: "{{ proxy_admin_password }}"

haproxy_pg_rw_port: "{{ proxy_pg_rw_port | default(5432) }}"
haproxy_pg_ro_port: "{{ proxy_pg_ro_port | default(5433) }}"

# Connection limits
haproxy_maxconn_global: 4096
haproxy_maxconn_frontend: 3000
haproxy_maxconn_backend: 1000

# Timeouts
haproxy_timeout_connect: "10s"
haproxy_timeout_client: "30m"
haproxy_timeout_server: "30m"

# Health check
haproxy_health_check_interval: "5s"
haproxy_health_check_fall: 3
haproxy_health_check_rise: 2
